type Iter_int(inner: Range_int);

func iter(r: Range_int) -> Iter_int {
    Iter_int { inner = r }
}

func next(iter: Iter_int) -> Maybe_int {
    // FIXME: Turn to iter.inner.current() once parsing is implemented for this
    current(iter.inner)
}
